# Lamda function is used to define the certain function with variables form thus to perform any task or to run the python programme
square=lambda a: a**2
print(square(6))

# Using method of lambda we can write a single line comment and can use it instead of as follows:
# def square(a):
#     return a*a
# print(square(8))
"""using lambda function is actually more preferable if you want to print single line code not a multi line code"""
sum=lambda x,y:(x+y)
print(sum(12,67))