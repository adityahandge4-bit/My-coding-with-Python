def sum(a,b):
    "This will sum the two numbers"
    c=a+b
    return c
print(sum.__doc__)
# To be noted __doc__ is being used for encoding the specific terms inside the file while writing the code so that we can get the info from the file and can use while writing the code
