list=["apple","banana","cherry"]
print(list[0]) # Use of slicing can be done inside the lists, tuples and strings
list[1]="orange"# can also be used to replace a certain letter inside the the list by using this way
print(list)
print(len(list))  # Also used to print length of the list
